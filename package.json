{
  "name": "360-test",
  "version": "1.0.0",
  "description": "This is some **Markdown** text",
  "main": "build.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build.js",
    "dev": "dev-refresh src --serve public --cmd 'npm run build'",
    "develop": "dev-refresh src --serve public --host 0.0.0.0 --port 8080 --cmd 'npm run build'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dashpilot/site-filmkit.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/dashpilot/site-filmkit/issues"
  },
  "homepage": "https://github.com/dashpilot/site-filmkit#readme",
  "dependencies": {
    "fs-extra": "^10.0.0",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "dev-refresh": "^1.2.1"
  }
}
